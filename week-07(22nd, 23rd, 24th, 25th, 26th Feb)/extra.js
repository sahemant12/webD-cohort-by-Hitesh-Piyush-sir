obj = {
    user: {
        address: {
            city: "New York"
        }
    }
}
let keypath = "user.address.city";
let keys = keypath.split(".");
let checkObj = obj;
//Good ques. Need thinking do it later.

